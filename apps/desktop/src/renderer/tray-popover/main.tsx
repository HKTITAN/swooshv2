// Placeholder — real tray popover lands at T700-T703.
const root = document.getElementById('root');
if (root) {
  root.textContent = 'Tray popover — placeholder. Real UI lands at T700.';
}
export {};
